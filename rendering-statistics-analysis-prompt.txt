I need you to analyze Sitecore `RenderingsStatistics.*.html` files in the current folder.

Context:
- These files are typically dumped every ~10 minutes as a rule, but the interval can differ depending on customer configuration.
- Filename format is: `RenderingsStatistics.YYYYMMDDZ.HHMMSSZ.html`.
- Time window must be selected from filename time (UTC).
- Statistics are cumulative per process, so analysis must use deltas between consecutive snapshots.
- If there is an app recycle/reset (sharp count drop), split into segments and aggregate deltas only within each segment.

Please do this:
1) Use `analyze_renderings_window.py` from this same folder (I copied it here intentionally).
2) Analyze one business day (Mon-Fri), defaulting to the first weekday with full coverage, and the window 10:00-12:00 UTC unless I specify otherwise.
3) Exclude:
   - Layout rows (`SxaLayout` / `CustomSxaLayout` etc.)
   - `DynamicPlaceholders.Controller.*`
4) Prioritize components that have:
   - high `Count`
   - high `Avg. time (ms)`
   - low `From cache` ratio (0 or low)
   - high `Avg. items`
5) Produce:
   - Top optimization candidates table
   - High `avg_items` stress points table
   - Grouped summary by component type (across all sites), with total count, weighted avg ms, cache ratio, avg items
   - Clear recommendations: "optimize code", "add/improve caching", or "monitor"

Output requirements:
- Show which date/window was used.
- Mention if recycle/segment split was detected.
- Keep findings practical and ranked (most impactful first).
- Call out components that appear repeatedly across many sites.
